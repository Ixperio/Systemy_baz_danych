@model IEnumerable<WebApplication4.Models.Zdarzenie>

@{
    ViewBag.Title = "Zdarzenia";
}

<h2>Zdarzenia</h2>

<p>
    @Html.ActionLink("Dodaj", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Id_Obywatela)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Data)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Id_Patrolu)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Id_Rodzaju)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Id_Obywatela)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Data)
        </td>
        <td>
             @Html.DisplayFor(modelItem => item.Id_Patrolu)
        </td>
            <td>
                @Html.DisplayFor(modelItem => item.Id_Rodzaju)
            </td>
            <td>
                @Html.ActionLink("Edytuj", "Edit", new { id=item.Id_Zdarzenia }) |
                @Html.ActionLink("Szczegóły", "Details", new { id=item.Id_Zdarzenia }) |
                @Html.ActionLink("Usuń", "Delete", new { id=item.Id_Zdarzenia })
            </td>
        </tr>
    }

    </table>
