@model IEnumerable<WebApplication4.Models.Funkcjonariusz_w_patrolu>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Funkcjonariusz.adres_Email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Patrol.Radiowozy_Policyjne_Nr_Rejestracyjny)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Funkcjonariusz.adres_Email)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Patrol.Radiowozy_Policyjne_Nr_Rejestracyjny)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
