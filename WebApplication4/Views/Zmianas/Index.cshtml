@model IEnumerable<WebApplication4.Models.Zmiana>

@{
    ViewBag.Title = "Spis zmian";
}

<h2>Spis zmian</h2>

<p>
    @Html.ActionLink("Dodaj nową", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Godzina_Rozpoczecia)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Godzina_Zakonczenia)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Godzina_Rozpoczecia)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Godzina_Zakonczenia)
        </td>
        <td>
            @Html.ActionLink("Edytuj", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Szczegóły", "Details", new { id=item.Id }) |
            @Html.ActionLink("Usuń", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
