@model IEnumerable<WebApplication4.Models.Patrol>

@{
    ViewBag.Title = "Patrole";
}

<h2>Spis patroli</h2>

<p>
    @Html.ActionLink("Dodaj nowy", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Data)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Radiowozy_Policyjne_Nr_Rejestracyjny)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Data)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Radiowozy_Policyjne_Nr_Rejestracyjny)
        </td>
        <td>
            @Html.ActionLink("Edytuj", "Edit", new { id=item.Id_Patrolu }) |
            @Html.ActionLink("Szczegóły", "Details", new { id=item.Id_Patrolu }) |
            @Html.ActionLink("Usuń", "Delete", new { id=item.Id_Patrolu })
        </td>
    </tr>
}

</table>
